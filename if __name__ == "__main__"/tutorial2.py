# we have written a function in tuturial1 and if we import than it executes the function and print in tutorial2
# so to stop the execution of the program we simply write if_name=='_main_'
import tutorial1
print(tutorial1.add(4,8))
